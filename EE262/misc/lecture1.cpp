#include <iostream>
using namespace std;

int main(int argc, char *argv[])
{
    //atoi is used a lot because programmers want to convert some argv values to int values and argv is always in cstring or char array, argv is array of cstrings btw

    
    return 0;
}